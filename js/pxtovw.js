//1px = 100vw / viewport's width (in px)

function convertPXToVW(px) {
	return px * (100 / document.documentElement.clientWidth);
}

var all = document.getSelection('style.css') 

elementList = parentNode.querySelectorAll();


all.convertPXToVW()